---
- name: "Apple macOS Workstation Bootstrap Playbook"
  hosts: localhost
  environment:
    PATH: "/opt/pkg/sbin:/opt/pkg/sbin:/opt/homebrew/sbin:/opt/homebrew/bin:{{ lookup('env', 'PATH') }}"
  roles:
    - { role: bootstrap_workstation_macos }
    - { role: geerlingguy.mac.dock }

# vim:ts=2:sw=2
